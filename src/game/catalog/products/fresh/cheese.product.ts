import { defineProduct } from '../../../definitions'
export default defineProduct({ key: 'cheese', category: 'fresh', name: 'Fromage', shortName: 'FROM', salePrice: 7.5, purchasePrice: 4.2, color: 0xfbbf24, shelfLifeDays: 10, requiresRefrigeration: true })
